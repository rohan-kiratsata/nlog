import React from "react";
import { cn } from "@/lib/utils";

export const MDXComponents = {
  // Example Usage of Custom Component:
  /* a: ({ className, ...props }: React.ComponentPropsWithoutRef<"a">) => (
    <a className={cn("hover:underline", className)} {...props} />
  ), */
  // Add any other custom components or overrides as needed
};
